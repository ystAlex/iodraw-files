```mermaid
flowchart TD A[节点集合] --> B[计算节点信誉值] B --> C[按信誉值降序排序] C --> D[初始化K个空分片] D --> E{遍历排序后节点列表} E --> F[选择当前总信誉值<br>最小的分片] F --> G[将当前节点分配至该分片] G --> H[更新分片总信誉值] H --> E E -- 所有节点分配完毕 --> I[输出分片结果]
```